class Solution(object):
    def addBinary(self, a, b):
        a , b = int(a,2) , int(b,2)
        while b:
            NoCarry = a ^ b 
            Carry = (a & b) << 1
            a , b = NoCarry , Carry
        return bin(a)[2:]
